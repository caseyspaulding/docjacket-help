---
title: Workflows
sidebar_label: Overview
sidebar_position: 1
description: Monitor automated workflows in DocJacket — document processing, email routing, compliance checks, and more.
---

import ScreenshotPlaceholder from '@site/src/components/ScreenshotPlaceholder';

<head>
<script type="application/ld+json">
{JSON.stringify({
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "What are workflows in DocJacket?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Workflows are automated background processes that handle routine operations — incoming email routing, document processing, compliance scanning, AI data extraction, reminder notifications, and morning briefings. They run automatically based on events in your transactions."
      }
    },
    {
      "@type": "Question",
      "name": "Can I create my own workflows in DocJacket?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "No. Workflows are built-in features that run behind the scenes. You don't configure them, but you can monitor them, approve or reject steps that need input, and retry failed steps from Settings > Workflows."
      }
    },
    {
      "@type": "Question",
      "name": "What does 'awaiting approval' mean in a DocJacket workflow?",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "Some workflows pause and wait for your input before continuing. For example, AI might extract data from a document and ask you to approve the results before saving. The workflow stays in Awaiting Approval status until you approve, reject, or cancel it."
      }
    }
  ]
})}
</script>
</head>

# Workflows

DocJacket runs automated workflows behind the scenes to handle routine operations — like processing incoming emails, running compliance checks, sending reminders, and extracting data from documents.

You don't create or configure workflows yourself. They run automatically based on events in your transactions. But you can **monitor** them and **approve or reject** steps that need your input.

## Monitoring workflows

Go to **Settings > Workflows** to see:

- **Running workflows** — What's currently being processed
- **Completed workflows** — What finished successfully
- **Failed workflows** — Anything that hit an error (with retry options)
- **Awaiting approval** — Steps that need your manual approval before continuing

<ScreenshotPlaceholder>workflow monitoring dashboard</ScreenshotPlaceholder>

## Approval steps

Some workflows pause and wait for your input. For example, AI might extract data from a document and ask you to approve the results before saving them. When this happens, you'll see the workflow in "Awaiting Approval" status.

From the workflow detail view, you can:

- **Approve** — Let the workflow continue with the suggested action
- **Reject** — Stop the workflow from proceeding
- **Retry** — Re-run a failed step
- **Cancel** — Stop the workflow entirely

## What workflows handle

Behind the scenes, workflows power features like:

- Document processing and compliance scanning
- Email intake and routing
- AI data extraction
- Reminder notifications
- Morning briefings
- Task and key date completion actions
