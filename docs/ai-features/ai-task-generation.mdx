---
sidebar_label: AI Task Generation
sidebar_position: 3
---

import ScreenshotPlaceholder from '@site/src/components/ScreenshotPlaceholder';

# AI Task Generation

Don't want to build your task checklist from scratch? Let AI analyze your transaction and suggest tasks for you.

## How it works

1. Open a transaction and go to the **Tasks** tab
2. Click the **AI Generate** button
3. AI looks at your transaction details — property type, state, parties involved, timeline — and suggests a task checklist
4. Review the suggestions — each task shows a name, priority, and estimated due date
5. Select the tasks you want to keep (or select all)
6. Click **Add Tasks**

That's it. The tasks appear in your list with priorities and due dates already set.

<ScreenshotPlaceholder>AI task generation suggestions with checkboxes</ScreenshotPlaceholder>

## What makes the suggestions good

AI doesn't just generate a generic checklist. It considers:

- **Transaction type** — A residential purchase has different tasks than a commercial lease
- **State-specific requirements** — Different states have different disclosure and compliance requirements
- **Your existing tasks** — AI avoids suggesting tasks you've already added
- **Timeline** — Due dates are calculated based on your contract and closing dates

## AI vs. Templates

Both AI generation and [task templates](./ai-task-generation.mdx) can populate your task list. Here's when to use each:

| Use AI generation when... | Use templates when... |
|--------------------------|----------------------|
| You want tasks tailored to this specific deal | You have a proven checklist you use every time |
| It's an unusual transaction type | You want consistent tasks across all deals |
| You're not sure what tasks you need | You've already customized a template you love |

You can also use both — apply a template first, then run AI generation to catch anything the template missed.
